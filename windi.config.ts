import { defineConfig } from "windicss/helpers";

export default defineConfig({
  darkMode: "class",
  attributify: false,

  theme: {
    fontFamily: {
      sans: ["Rubik", "sans-serif"]
    }
  }
});

